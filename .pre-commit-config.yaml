repos:
  - repo: local
    hooks:
      - id: doc-index
        name: Rebuild markdown doc index
        entry: .claude/hooks/run-doc-index.sh --check
        language: system
        files: \.md$
        pass_filenames: false
